services:
  - type: web
    name: mini-remini-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: standard
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: "8000"
      - key: DEBUG
        value: "false"
    healthCheckPath: /health
